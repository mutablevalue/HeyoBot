For whatever updates you make PLEASE make sure the configuration revolves around config.yaml

That means role permission, access permissions and stuff like that, everything secure needs to be in that yaml


To run:

npm i (to install dependencies)
npm run build (to build ts)
npm start (to start bot)